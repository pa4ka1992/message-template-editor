export enum BUTTON_COLOR {
  def = '--primary-color-75',
  light = '--text-color-50',
  dark = '--primary-color-100',
  red = '--danger-color',
  green = '--success-color',
  blue = '--secondary-color-100'
}
